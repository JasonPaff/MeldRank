/**
 * The DeclareTrump phase driver: `declareTrump` legality (contract winner naming
 * a real suit) and the recorded trump, per design decision 3.
 */
export { declareTrump, type DeclareStep } from './declare';
